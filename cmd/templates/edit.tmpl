{{define "title"}}Editing {{.Title}} &ndash; setonotes{{end}}
{{define "content"}}
<link rel="stylesheet" href="/static/css/easymde.min.css">
<script src="/static/js/easymde.min.js" defer></script>
<script src="/static/js/editor.js" defer></script>
<form id="input-form" action="/save/{{ .ID }}" method="POST">
<div style="display: none">
    <textarea id="input-title" name="title" rows="1" cols="40">{{printf "%s" .Title}}</textarea>
</div>
<div>
    <textarea id="input-body" name="body" style="display: none">{{printf "%s" .Body}}</textarea>
</div>
</form>
{{end}}
